name: http_cache
description: Caches HTTP requests for a Flutter app.
version: 2.0.1

environment:
  sdk: '>=2.18.6 <3.0.0'

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  hive_flutter: ^1.1.0
  json_serializable: ^6.6.0
  hive_storage_service:
    git:
      url: https://github.com/gnarhard/hive_storage_service.git
      ref: main
  http_service:
    git:
      url: https://github.com/gnarhard/http_service.git
      ref: main
  json_annotation: ^4.8.0
  build_runner: ^2.3.3
  hive: ^2.2.3
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  get_it: ^7.2.0
  faker: ^2.1.0

dependencies:
  http: ^0.13.5
  flutter:
    sdk: flutter
